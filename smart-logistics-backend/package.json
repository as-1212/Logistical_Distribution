{
  "name": "smart-logistics-backend",
  "version": "1.0.0",
  "description": "Backend API for Smart Resource Allocation AI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "start:backend": "node server.js",
    "build:prod": "npm install --production && npm run build:services",
    "build:services": "npm run build:backend && npm run build:frontend && npm run build:ml",
    "build:backend": "echo 'Backend ready'",
    "build:frontend": "cd ../smart-logistics-frontend && npm install --production",
    "build:ml": "cd ../smart-logistics-ml && pip install -r requirements.txt",
    "start:services": "npm run start:backend"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "helmet": "^6.1.5",
    "morgan": "^1.10.0",
    "socket.io": "^4.6.2",
    "python-shell": "^5.0.0",
    "multer": "^1.4.5-lts.1",
    "csv-parser": "^3.0.0",
    "joi": "^17.9.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "jest": "^29.5.0"
  },
  "keywords": [
    "logistics",
    "ai",
    "machine-learning",
    "supply-chain",
    "retail"
  ],
  "author": "Smart Logistics Team",
  "license": "MIT"
}
